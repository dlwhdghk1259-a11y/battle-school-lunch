# 코딩 에이전트 지침

## 프로젝트 개요

이 저장소는 NEIS 공개 API를 활용해 초·중·고등학교의 급식 메뉴를 조회하고,
급식 정보를 분석하는 애플리케이션을 단계적으로 만드는 워크숍 프로젝트입니다.
현재 저장소에는 API 원본 데이터와 워크숍 문서가 중심이며, 애플리케이션 코드는
워크숍 단계에 따라 `frontend/`, `backend/` 등에 추가될 수 있습니다.

## 일반 작업 지침

- 작업을 시작하기 전에 `README.md`, 관련 `docs/` 문서, 그리고 변경 대상
  디렉터리의 기존 규칙과 구현을 확인합니다.
- 저장소의 기존 규칙과 자동화 설정을 이 문서보다 우선합니다. 서로 충돌하면
  더 구체적이고 가까운 디렉터리의 지침을 따릅니다.
- 요청된 범위에 집중하고, 동작과 무관한 리팩터링이나 불필요한 추상화를
  추가하지 않습니다.
- 기존 구조와 이름, 포맷, 의존성을 우선 재사용합니다. 새로운 라이브러리나
  설정을 추가할 때는 필요한 이유와 영향을 확인합니다.
- 동작이나 설정이 바뀌면 관련 문서와 예제를 함께 갱신합니다.
- API 명세(`data/openapi.json`)와 워크숍 문서의 의도를 임의로 바꾸지 않습니다.

## Python 및 TypeScript 가이드라인

- Python과 TypeScript 모두 입력과 반환값을 명시하고, 가능한 한 엄격한 타입
  검사를 유지합니다. 타입 오류를 무시하기 위한 광범위한 캐스팅이나 예외
  무시는 사용하지 않습니다.
- 외부 API, 파일, 사용자 입력은 신뢰할 수 없는 데이터로 취급하고 경계에서
  검증합니다. 누락되거나 잘못된 값은 호출자가 원인을 알 수 있도록 명확하게
  처리합니다.
- 예외를 광범위하게 잡아 숨기지 말고, 프로젝트의 기존 오류 처리와 로깅
  방식을 따릅니다. 실패를 성공처럼 반환하는 기본값을 만들지 않습니다.
- 비동기 코드와 리소스 관리는 기존 패턴을 따르며, 네트워크 요청에는
  프로젝트가 정한 타임아웃·재시도·취소 정책이 있으면 이를 적용합니다.
- 의존성은 각 구성 요소의 기존 매니페스트와 잠금 파일을 통해 관리하고,
  필요한 경우에만 최소 범위로 변경합니다.

## 테스트 및 검증

- 변경에 직접 관련된 테스트를 추가하거나 수정하고, 변경 전에 존재하던
  테스트도 유지합니다.
- 저장소에 실제로 구성된 테스트, 포맷터, 린터, 타입 검사 및 빌드 명령을
  확인한 뒤 실행합니다. 아직 구성되지 않은 도구나 명령을 임의로 만들거나
  필수 검사로 지정하지 않습니다.
- 현재 CI 설정이 조건부로 실행하는 검사를 참고합니다. 프론트엔드는
  `frontend/package-lock.json`, 백엔드는 `backend/pyproject.toml`이 있을
  때 해당 검사가 활성화됩니다.
- 검사를 실행할 수 없는 경우에는 그 이유와 실행하지 못한 검사를 변경
  설명에 명시합니다.

## 보안

- 비밀 값, API 키, 토큰, 개인 정보를 코드·로그·문서·커밋에 포함하지
  않습니다. 로컬 설정 파일과 생성된 인증 정보는 커밋하지 않습니다.
- 사용자 입력과 외부 API 응답을 검증하고, 로그에 민감한 값이 남지 않도록
  합니다. 의존성 변경은 알려진 보안 영향과 라이선스를 확인합니다.
- 보안 취약점은 공개 이슈나 Pull Request에 상세 내용을 작성하지 말고
  `SECURITY.md`의 비공개 신고 절차를 따릅니다.

## 문서화

- 공개 동작, 설정, API 계약 또는 개발 절차를 변경하면 관련 `README.md`,
  `docs/`, `CONTRIBUTING.md`를 함께 갱신합니다.
- 문서는 현재 코드와 명령을 정확히 반영해야 하며, 아직 존재하지 않는
  기능이나 도구를 사용 가능한 것처럼 안내하지 않습니다.
- 기존 문서의 언어, 용어, 링크 형식과 예제 스타일을 유지합니다.

## 주의사항과 가드레일

- 요청 범위를 벗어난 파일, 데이터, 워크플로를 변경하지 않습니다.
- 기존 작업 중인 변경사항을 덮어쓰거나 되돌리지 않습니다. 충돌이 있으면
  먼저 상태를 확인하고 변경 의도를 보존합니다.
- 삭제, 데이터 변환, 의존성 업그레이드처럼 되돌리기 어려운 작업은 영향
  범위를 확인한 뒤 최소한으로 수행합니다.
- 생성된 산출물, 운영 비밀, 개인별 IDE 설정은 저장소에 추가하지 않습니다.

## Git 커밋 및 Pull Request

- 커밋 메시지는 저장소의 [Conventional Commits](https://www.conventionalcommits.org/)
  규칙을 따릅니다. 예: `feat:`, `fix:`, `docs:`, `test:`, `refactor:`,
  `chore:`.
- 하나의 커밋과 Pull Request는 하나의 논리적 변경에 집중합니다. 관련 없는
  정리나 포맷 변경을 섞지 않습니다.
- Pull Request를 만들 때는 반드시 `.github/PULL_REQUEST_TEMPLATE.md`를
  활용해서 작성하고, 템플릿의 모든 관련 항목을 빠짐없이 작성합니다.
- Pull Request 본문에서 관련 이슈는 `Closes #123` 형식으로 연결하고, 동작
  또는 설정 변경 시 관련 문서와 테스트가 반영되었는지 확인합니다.
