# 급식 배틀 - 학교 급식 조회 앱

## 1. 문서 개요

### 1.1 목적

이 문서는 **급식 배틀 - 학교 급식 조회 앱**의 제품 요구사항을 정의한다. 사용자가 학교를 쉽게 찾고 날짜 범위를 지정하면, 선택한 학교의 중식 메뉴를 날짜별로 확인할 수 있어야 한다. 이 문서는 이후 프론트엔드와 백엔드 구현의 기준이 되는 제품 요구사항의 단일 출처다.

### 1.2 범위

- React 기반 웹 프론트엔드와 Python 기반 백엔드로 구성한다.
- 학교명 일부를 이용한 학교 검색과 학교 선택을 제공한다.
- 선택한 학교와 날짜 범위에 해당하는 **중식** 급식 정보를 날짜별로 표시한다.
- NEIS API의 학교기본정보 및 급식식단정보를 사용한다.
- 프론트엔드와 백엔드의 전체 사용자 흐름을 E2E 테스트할 수 있어야 한다.

다음 항목은 이번 제품 범위에 포함하지 않는다.

- 조식·석식·학사 일정 조회
- 사용자 회원가입, 로그인, 즐겨찾기 또는 급식 데이터 저장
- 학교 간 급식 비교·평가 기능
- 프론트엔드의 NEIS API 직접 호출

## 2. 제품 목표와 성공 기준

### 2.1 개발 목적

NEIS 공개 API의 원시적인 조회 방식을 사용자가 이해하기 쉬운 웹 흐름으로 감싸 학교 급식 정보를 빠르게 확인하게 한다. 학교명과 날짜만 입력하면 복잡한 교육청 코드나 NEIS 응답 형식을 몰라도 결과를 확인할 수 있어야 한다.

### 2.2 대상 사용자

- 오늘 또는 특정 기간의 급식을 확인하려는 초·중·고 학생
- 자녀의 학교 급식을 확인하려는 보호자
- 학교 급식 정보를 간단히 조회하려는 교직원 및 일반 사용자

### 2.3 성공 기준

- 사용자는 학교명 일부로 검색해 원하는 학교를 선택할 수 있다.
- 사용자는 유효한 날짜 범위를 선택하고 조회를 요청할 수 있다.
- 조회 결과는 날짜별 중식 메뉴로 구분되어 표시된다.
- 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음 및 API 오류를 사용자가 이해할 수 있는 메시지로 확인할 수 있다.
- 사용자는 NEIS 인증키나 내부 API 파라미터를 입력하지 않아도 된다.

## 3. 핵심 사용자 흐름

### 3.1 학교 검색

1. 사용자가 학교명 입력란에 학교 이름의 일부를 입력한다.
2. 사용자가 검색을 실행한다.
3. 앱은 일치하는 학교 목록을 표시한다.
4. 각 결과에는 학교명, 학교 종류, 시도 또는 주소 등 학교를 구분할 수 있는 정보가 표시된다.
5. 사용자가 하나의 학교를 선택한다.

학교가 선택되기 전에는 날짜 선택과 급식 조회를 완료할 수 없어야 한다. 검색어가 지나치게 짧거나 공백뿐인 경우에는 검색을 실행하지 않고 입력 오류를 안내한다.

### 3.2 날짜 범위 선택

1. 사용자가 시작일과 종료일을 선택한다.
2. 앱은 시작일이 종료일보다 늦지 않은지 검증한다.
3. 사용자가 급식 조회를 실행한다.

초기 날짜 값은 사용자가 현재 날짜를 기준으로 쉽게 조회할 수 있도록 제공할 수 있지만, 실제 초기값과 최대 조회 기간은 구현 시 제품 정책으로 고정하고 문서화해야 한다. 날짜는 사용자의 지역 시간 기준으로 해석하며, 날짜만 입력받고 시간은 받지 않는다.

### 3.3 날짜별 급식 결과 표시

1. 백엔드는 선택한 학교의 교육청 코드와 학교 코드를 사용해 NEIS 급식식단정보를 조회한다.
2. 중식 식사 코드만 결과에 포함한다.
3. 프론트엔드는 선택한 날짜 범위 내 결과를 날짜 오름차순으로 표시한다.
4. 각 날짜의 카드 또는 행에는 날짜와 중식 메뉴를 표시한다.
5. 메뉴 데이터에 포함된 알레르기 표기, 원산지, 칼로리와 영양정보가 있으면 함께 표시할 수 있다. 원문이 없거나 비어 있는 항목은 빈 값으로 오해되지 않도록 표시하지 않는다.

## 4. 기능 요구사항

### FR-1. 학교명 부분 검색

- 사용자는 학교명 전체가 아닌 일부 문자열로 검색할 수 있어야 한다.
- 검색은 백엔드 API를 통해 수행하며 프론트엔드는 NEIS API를 직접 호출하지 않는다.
- 결과가 많을 경우 페이지네이션 또는 결과 수 제한을 적용하고, 사용자가 결과를 구분할 수 있는 학교 정보를 제공한다.
- 검색 중에는 진행 상태를 표시하고, 반복 요청으로 중복 결과나 경합 상태가 발생하지 않도록 한다.

### FR-2. 학교 선택

- 사용자는 검색 결과에서 정확히 하나의 학교를 선택할 수 있어야 한다.
- 선택된 학교에는 학교명과 함께 교육청 코드 및 학교 코드가 내부적으로 보존되어야 한다.
- 검색 결과가 갱신되면 이전 선택을 무효화해 다른 학교의 급식이 조회되지 않도록 한다.

### FR-3. 날짜 범위 선택

- 사용자는 시작일과 종료일을 입력하거나 날짜 선택 UI로 지정할 수 있어야 한다.
- 시작일과 종료일은 모두 유효한 날짜여야 한다.
- 시작일은 종료일보다 늦을 수 없다.
- 학교가 선택되지 않았거나 날짜가 유효하지 않으면 급식 조회 요청을 보내지 않는다.

### FR-4. 중식 급식 조회

- 백엔드는 선택된 학교 코드, 교육청 코드, 시작일, 종료일을 받아 NEIS 급식식단정보를 조회한다.
- 조회 시 식사 구분을 중식으로 제한한다.
- 백엔드는 NEIS 응답을 앱에서 사용하는 안정적인 응답 구조로 변환해 프론트엔드에 반환한다.
- 선택한 범위에 해당하는 여러 날짜의 결과를 한 번의 조회 흐름으로 제공한다.

### FR-5. 결과 표시

- 결과는 날짜별로 그룹화하고 날짜 오름차순으로 표시한다.
- 각 날짜에 중식 정보가 없으면 해당 날짜에 급식 정보가 없음을 명확히 표시한다.
- 메뉴 문자열의 줄바꿈, 알레르기 번호 등 NEIS 원문 형식은 읽기 쉬운 형태로 표시하되 정보 자체를 임의로 삭제하지 않는다.
- 결과가 로딩 중이면 조회 중 상태를, 완료 후에는 마지막 조회 조건과 결과 상태를 알 수 있어야 한다.

### FR-6. 오류와 예외 상황

- **검색 결과 없음:** 입력한 검색어에 해당하는 학교가 없다는 안내와 다른 검색어를 입력할 수 있는 상태를 제공한다.
- **잘못된 날짜 범위:** 시작일·종료일의 누락, 형식 오류, 시작일이 종료일보다 늦은 경우를 제출 전에 안내한다.
- **급식 정보 없음:** 선택 범위에 중식 자료가 없다는 안내를 표시한다. 이를 시스템 오류로 표현하지 않는다.
- **잘못된 학교 선택 또는 요청:** 만료되었거나 유효하지 않은 학교 식별자는 조회하지 않고 오류를 안내한다.
- **NEIS 인증·권한 오류:** 내부 인증정보를 노출하지 않고 일시적으로 조회할 수 없다는 일반 메시지를 표시한다.
- **호출 제한·네트워크 오류:** 재시도할 수 있다는 안내를 표시하되 무한 자동 재시도는 하지 않는다.
- **서버 오류:** 사용자가 다음 행동을 알 수 있는 일반 오류 메시지를 표시하고 상세 스택이나 비밀 값을 노출하지 않는다.

## 5. 비기능 요구사항

- 반응형 UI를 제공해 데스크톱과 모바일 화면에서 핵심 흐름을 사용할 수 있어야 한다.
- 검색 입력, 날짜 입력, 버튼, 결과 상태는 키보드로 사용할 수 있고 적절한 레이블과 오류 안내를 제공해야 한다.
- NEIS 인증키와 내부 운영 설정은 사용자에게 노출하지 않는다.
- 외부 API 장애가 프론트엔드의 비정상 종료로 이어지지 않도록 백엔드가 오류를 일관된 형식으로 변환한다.
- 날짜와 학교 식별자는 요청·응답에서 일관된 형식으로 유지한다.

## 6. 제품 수용 기준

- [ ] 리포지토리 루트에 이 문서와 `TRD.md`가 존재한다.
- [ ] 앱 제목, 개발 목적, 대상 사용자가 문서에 정의되어 있다.
- [ ] 학교명 부분 검색 → 학교 선택 → 날짜 범위 선택 → 날짜별 중식 결과 표시의 전체 흐름이 정의되어 있다.
- [ ] 부분 학교명 검색, 학교 선택, 날짜 범위 선택, 급식 조회 요구사항이 정의되어 있다.
- [ ] 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음 및 외부 API 오류 처리가 정의되어 있다.
- [ ] 프론트엔드와 백엔드가 분리되고 프론트엔드가 NEIS API를 직접 호출하지 않는다는 제약이 반영되어 있다.

