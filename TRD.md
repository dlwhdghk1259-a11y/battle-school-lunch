# 급식 배틀 - 학교 급식 조회 앱 기술 요구사항

## 1. 문서 개요

이 문서는 `PRD.md`에 정의된 급식 조회 앱을 구현하기 위한 기술 설계와 운영 경계를 정의한다. 이번 작업에서는 설계 문서만 작성하며 애플리케이션 코드, Docker 설정, 테스트 코드는 포함하지 않는다.

## 2. 시스템 구성

애플리케이션은 Docker Compose로 함께 실행하는 다음 두 서비스로 구성한다.

```text
사용자 브라우저
      │
      ▼
React 프론트엔드 ── 앱 내부 HTTP API ──► Python 백엔드 ──► NEIS 공개 API
      ▲                                      │
      └──────────── 정규화된 JSON 응답 ◄──────┘
```

### 2.1 프론트엔드

- React 기반의 단일 웹 UI를 제공한다.
- 학교 검색, 학교 선택, 날짜 범위 선택, 결과 표시의 상태와 화면 전환을 담당한다.
- `src/openapi.json`에 정의한 앱 내부 백엔드 계약을 기준으로 별도의 API 클라이언트 코드를 사용한다.
- `data/openapi.json`에 정의된 NEIS 엔드포인트를 직접 호출하지 않는다.
- 로딩, 빈 결과, 검증 오류, 서버 오류를 상태별로 구분해 표시한다.

### 2.2 백엔드

- Python 기반 HTTP API를 제공한다.
- 프론트엔드 요청의 형식과 날짜 범위, 학교 식별자를 경계에서 검증한다.
- `src/openapi.json`의 앱 내부 계약을 구현한다.
- `data/openapi.json`의 NEIS 학교기본정보(`/hub/schoolInfo`) 및 급식식단정보(`/hub/mealServiceDietInfo`) 명세를 근거로 별도의 외부 API 클라이언트를 구현한다.
- NEIS 인증키, 기본 요청 파라미터, 타임아웃과 외부 오류 매핑을 백엔드에서 관리한다.
- 외부 응답의 NEIS 고유 중첩 구조를 프론트엔드에 그대로 노출하지 않고 앱의 안정적인 응답 모델로 변환한다.

## 3. API 계약과 데이터 흐름

### 3.1 계약 문서

- `data/openapi.json`: 저장소에 제공된 NEIS 외부 API 명세다. 학교 정보와 급식 식단 정보의 외부 요청·응답 모델을 정의한다.
- `src/openapi.json`: 프론트엔드와 백엔드 사이의 앱 내부 API 명세다. 구현 단계에서 별도로 생성하며 외부 NEIS 계약과 혼동하지 않는다.
- 두 명세의 서버 URL, 인증 방식, 오류 모델은 서로 독립적으로 관리한다.

### 3.2 학교 검색 흐름

1. 프론트엔드는 학교명 일부를 내부 학교 검색 엔드포인트에 전달한다.
2. 백엔드는 입력을 검증하고 `data/openapi.json`의 `getSchoolInfo` 요청으로 `SCHUL_NM` 조건을 사용한다.
3. 백엔드는 학교명, 시도교육청 코드(`ATPT_OFCDC_SC_CODE`), 학교 행정표준코드(`SD_SCHUL_CODE`), 학교 종류와 주소 등 선택에 필요한 필드를 정규화한다.
4. 프론트엔드는 정규화된 목록만 렌더링하고, 선택 시 코드 두 개를 내부 상태에 저장한다.

### 3.3 급식 조회 흐름

1. 프론트엔드는 선택한 학교의 내부 식별자와 `startDate`, `endDate`를 내부 급식 조회 엔드포인트에 전달한다.
2. 백엔드는 날짜를 `YYYYMMDD`로 변환하고, `ATPT_OFCDC_SC_CODE`와 `SD_SCHUL_CODE`를 외부 요청에 포함한다.
3. 백엔드는 중식 식사 구분 코드(`MMEAL_SC_CODE`)를 사용해 조식·석식을 제외하고 `MLSV_FROM_YMD`, `MLSV_TO_YMD`로 범위를 조회한다.
4. 백엔드는 NEIS의 `mealServiceDietInfo` 행을 날짜별 급식 객체로 변환한다.
5. 프론트엔드는 날짜 오름차순으로 결과를 표시한다. 결과가 없는 날짜와 범위 전체가 비어 있는 경우를 구분할 수 있도록 응답의 의미를 유지한다.

### 3.4 내부 API 모델 원칙

내부 API는 구현 시 다음 의미를 표현할 수 있어야 한다.

- 학교 검색 요청: 공백을 제거한 부분 학교명과 선택적 페이지 정보
- 학교 검색 결과: 사용자 표시용 학교 정보와 후속 급식 조회에 필요한 교육청·학교 코드
- 급식 조회 요청: 학교 식별자, 시작일, 종료일
- 급식 결과: 날짜, 식사 구분, 메뉴, 선택적 원산지·칼로리·영양정보
- 오류 응답: 기계 판별 가능한 오류 코드와 사용자 표시용 메시지

내부 계약은 날짜 형식을 ISO 날짜(`YYYY-MM-DD`)로 통일하고, NEIS의 `YYYYMMDD` 형식은 백엔드 외부 연동 경계에서만 사용한다. 식사 코드와 같은 외부 코드 값은 프론트엔드가 직접 구성하지 않도록 한다.

## 4. 검증 및 오류 처리

### 4.1 입력 검증

- 학교 검색어는 공백 제거 후 비어 있지 않아야 한다.
- 학교 식별자는 백엔드가 발급·검증한 코드 조합이어야 한다.
- 날짜는 실제 달력에 존재하는 ISO 날짜여야 한다.
- 시작일은 종료일보다 늦을 수 없다.
- 외부 API가 요구하는 범위 파라미터와 단일 날짜 파라미터를 한 요청에 혼합하지 않는다.

### 4.2 오류 매핑

백엔드는 NEIS의 응답 코드와 HTTP 오류를 내부 오류 코드로 매핑한다. 인증 실패나 호출 제한처럼 운영 정보가 포함될 수 있는 오류는 안전한 사용자 메시지만 반환하고 인증키, 요청 URL의 비밀 값, 스택 트레이스를 반환하지 않는다. 네트워크 타임아웃은 제한된 정책으로 처리하며 무한 재시도나 오류를 성공 응답으로 바꾸는 기본값을 사용하지 않는다.

검색 결과가 없거나 급식 행이 없는 정상 응답은 오류 상태가 아니라 빈 목록과 명시적인 결과 상태로 표현한다. 프론트엔드는 이 상태를 오류 화면과 구분한다.

## 5. Docker Compose 실행 설계

- Compose 파일은 프론트엔드와 백엔드 서비스를 정의한다.
- 프론트엔드는 백엔드의 내부 서비스 주소를 환경 설정으로 주입받으며 브라우저에서 NEIS 주소를 직접 참조하지 않는다.
- 백엔드는 외부 NEIS 기본 URL과 인증키를 환경 변수 또는 비밀 관리 방식으로 주입받는다. 인증키는 이미지, 소스 코드, 로그에 포함하지 않는다.
- 서비스 간 통신 포트와 외부 공개 포트는 환경별로 설정 가능해야 한다.
- 프론트엔드가 백엔드 준비 전에 시작할 수 있으므로 헬스체크 또는 준비 상태 확인을 사용한다. 단, 클라이언트의 무한 대기나 무한 재시도는 허용하지 않는다.
- 개발·테스트·운영 설정은 필요한 환경 변수와 기본값을 문서화하되 실제 비밀 값은 커밋하지 않는다.

## 6. 테스트 전략

### 6.1 프론트엔드 통합 테스트

프론트엔드 테스트는 컴포넌트 내부 구현보다 사용자 흐름과 백엔드 API 경계를 검증하는 통합 테스트를 중심으로 작성한다.

- 부분 학교명 입력 후 검색 결과가 표시되는지 검증한다.
- 검색 결과에서 학교를 선택하고 날짜 범위를 입력하는지 검증한다.
- 급식 조회 요청의 학교 식별자와 날짜가 내부 API 클라이언트에 올바르게 전달되는지 검증한다.
- 날짜별 중식 결과, 빈 결과, 로딩 및 오류 상태가 표시되는지 검증한다.
- 프론트엔드 테스트에서 실제 NEIS API를 호출하지 않고 내부 API 응답을 모킹한다.

### 6.2 백엔드 단위 테스트

- 날짜 형식과 날짜 범위 검증
- 학교 검색어 정규화
- NEIS 학교·급식 응답의 정상 변환
- 중식 필터와 날짜별 그룹화
- NEIS 응답 코드, 인증 오류, 호출 제한, 타임아웃의 내부 오류 매핑
- 빈 `row` 응답을 정상적인 빈 결과로 처리하는 로직

외부 HTTP 호출은 단위 테스트에서 모킹하고, 인증키나 개인 데이터를 테스트 fixture에 포함하지 않는다.

### 6.3 백엔드 통합 테스트

백엔드 HTTP 엔드포인트를 실제로 구동해 요청 검증, 외부 API 클라이언트, 응답 직렬화가 연결되는지 검증한다. 외부 NEIS 서비스에 의존하는 테스트와 재현 가능한 모의 서버 기반 테스트를 구분하고, 기본 테스트는 모의 응답으로 결정적으로 실행한다.

### 6.4 E2E 테스트

Docker Compose로 프론트엔드와 백엔드를 함께 실행한 뒤 브라우저에서 전체 흐름을 검증한다.

1. 학교명 일부로 검색한다.
2. 검색 결과에서 학교를 선택한다.
3. 유효한 날짜 범위를 선택한다.
4. 조회를 실행하고 날짜별 중식 메뉴를 확인한다.

추가 시나리오로 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음, 백엔드 오류 시 사용자 메시지와 재시도 가능한 상태를 검증한다. E2E에서도 실제 인증키를 사용하지 않으며, 외부 NEIS 의존성은 테스트용 응답으로 통제한다.

## 7. 보안·운영 고려사항

- NEIS API 인증키는 서버 측 비밀로만 관리한다.
- 요청 로그에는 인증키, 전체 외부 URL, 사용자 입력의 민감한 값이 남지 않도록 한다.
- 프론트엔드가 임의의 외부 URL을 호출할 수 있는 프록시로 동작하지 않도록 백엔드 엔드포인트와 파라미터를 제한한다.
- 외부 데이터는 신뢰하지 않고 스키마와 필수 식별자를 검증한다.
- 브라우저에는 필요한 학교·급식 데이터만 전달하고 내부 설정이나 외부 오류 상세를 노출하지 않는다.

## 8. 구현 완료 기준

- [ ] React 프론트엔드와 Python 백엔드의 책임이 분리되어 있다.
- [ ] 프론트엔드는 NEIS API를 직접 호출하지 않고 내부 API 클라이언트를 사용한다.
- [ ] `data/openapi.json`을 근거로 한 NEIS 외부 연동과 `src/openapi.json`을 통한 내부 계약이 구분되어 있다.
- [ ] 학교 검색부터 날짜별 중식 결과까지의 데이터 흐름이 구현 설계에 반영되어 있다.
- [ ] 프론트엔드·백엔드가 Docker Compose로 빌드·실행되는 구성이 정의되어 있다.
- [ ] 프론트엔드 통합 테스트, 백엔드 단위·통합 테스트, 전체 흐름 E2E 테스트 전략이 정의되어 있다.
